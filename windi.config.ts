import { defineConfig } from 'windicss/helpers'

export default defineConfig({
    jit: true,
    theme: {
        extend: {
            fontFamily: {
                main: ["Inter"]
            }
        }
    }
})
